# Exemplo de arquivo .env para Maint_Control
# Copie este arquivo para .env e ajuste os valores conforme necessário

# ========================================
# CONFIGURAÇÃO DO BANCO DE DADOS
# ========================================

# Hostname do banco de dados
# Para Docker: use 'db' (nome do serviço no docker-compose.yml)
# Para desenvolvimento local: use '127.0.0.1' ou 'localhost'
DB_HOST=db

# Nome do banco de dados
DB_NAME=maintcontrol_db

# Usuário do banco de dados
DB_USER=root

# Senha do banco de dados
# IMPORTANTE: Altere esta senha em produção!
DB_PASS=rootpassword

# ========================================
# CONFIGURAÇÃO DO MYSQL (Docker Compose)
# ========================================

# Senha do usuário root do MySQL
MYSQL_ROOT_PASSWORD=rootpassword

# Nome do banco a ser criado automaticamente
MYSQL_DATABASE=maintcontrol_db

# ========================================
# OBSERVAÇÕES
# ========================================

# 1. Este arquivo NÃO deve ser commitado no repositório
#    (já está incluído no .gitignore e .dockerignore)
#
# 2. Para usar este arquivo com docker-compose, 
#    adicione a seguinte linha no docker-compose.yml:
#    
#    services:
#      web:
#        env_file:
#          - .env
#      db:
#        env_file:
#          - .env
#
# 3. Para desenvolvimento local sem Docker,
#    o arquivo backend/conexao.php usará valores padrão
#    se as variáveis de ambiente não estiverem definidas
